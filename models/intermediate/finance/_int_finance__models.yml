models:
  - name: int_shop_enriched_revenue
    description: Shop details enriched with revenue data
    tests:
      - dbt_utils.unique_combination_of_columns:    
          arguments:
            combination_of_columns:
              - order_id
              - shop_id
